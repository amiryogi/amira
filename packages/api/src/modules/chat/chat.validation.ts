/**
 * Chat validation schemas — re-exported from @amira/shared.
 * Single source of truth for all Zod schemas.
 */
export { sendMessageSchema, joinChatSchema } from '@amira/shared';
export type { SendMessageSchemaType, JoinChatSchemaType } from '@amira/shared';
