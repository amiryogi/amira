export enum ChatRoomStatus {
  OPEN = 'open',
  CLOSED = 'closed',
}

export enum MessageStatus {
  SENT = 'sent',
  DELIVERED = 'delivered',
  READ = 'read',
}

export enum SenderRole {
  CUSTOMER = 'customer',
  ADMIN = 'admin',
}
